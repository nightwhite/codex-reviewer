{
  "name": "codex-reviewer",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc && esbuild src/main.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/main.cjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "@octokit/rest": "^21.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "esbuild": "^0.28.0",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@10.33.0"
}
